\uuid{3941}
\titre{Propriété des valeurs intermédiaires pour $f'$}
\theme{}
\auteur{quercia}
\date{2010/03/11}
\organisation{exo7}
\contenu{
  \texte{Soit $f : {[a,b]} \to \R$ dérivable.}
\begin{enumerate}
  \item \question{On note $E = \{(x,y) \in {[a,b]}^2 \text{ tq } x < y\}$ et pour $(x,y) \in E$ :
    $\varphi(x,y) = \frac{f(x)-f(y)}{x-y}$.
    Montrer que $\varphi(E)$ est un intervalle.}
  \item \question{En déduire que $f'([a,b])$ est un intervalle.}
\end{enumerate}
\begin{enumerate}

\end{enumerate}
}