\uuid{viKm}
\exo7id{4014}
\titre{Inégalité sur $f'$}
\theme{Exercices de Michel Quercia, Formules de Taylor}
\auteur{quercia}
\date{2010/03/11}
\organisation{exo7}
\contenu{
  \texte{Soit $f : {\R} \to {\R^+}$ une fonction de classe $\mathcal{C}^2$.
On suppose : $\forall\ x \in \R,\ |f''(x)| \le M$.}
\begin{enumerate}
  \item \question{Montrer que : $\forall\ x,y \in \R,\ f(x) + yf'(x) + \frac {y^2}2M \ge 0$.}
  \item \question{En déduire que : $\forall\ x \in \R,\ |f'(x)| \le \sqrt{2Mf(x)}$.}
  \item \question{On suppose que : $\forall\ x \in \R,\ |f'(x)| = \sqrt{2Mf(x)}$.
    Que pouvez-vous dire de $f$ ?}
\end{enumerate}
\begin{enumerate}
  \item \reponse{$= f(x+y) + \frac {y^2}2(M-f''(z))$.}
  \item \reponse{$\Delta \le 0$.}
  \item \reponse{$\sqrt f$ est affine.}
\end{enumerate}
}