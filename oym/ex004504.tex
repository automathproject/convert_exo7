\exo7id{4504}
\titre{\'Etude de convergence}
\theme{}
\auteur{quercia}
\date{2010/03/14}
\organisation{exo7}
\contenu{
  \texte{On pose $f_n(x) = x^n(1-x)$ et $g_n(x) = x^n\sin(\pi x)$.}
\begin{enumerate}
  \item \question{Montrer que la suite $(f_n)$ converge uniformément vers la fonction nulle sur
    $[0,1]$.}
  \item \question{En déduire qu'il en est de même pour la suite $(g_n)$.
    (On utilisera la concavité de sin sur $[0,\pi]$)}
\end{enumerate}
\begin{enumerate}

\end{enumerate}
}