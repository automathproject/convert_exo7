\uuid{ZwyM}
\exo7id{3867}
\titre{$f(x+y) = f(x) + f(y)$}
\theme{Exercices de Michel Quercia, Fonctions continues}
\auteur{quercia}
\date{2010/03/11}
\organisation{exo7}
\contenu{
  \texte{Soit $f : {\R} \to {\R}$ telle que : $\forall\ x,y \in \R,\ f(x+y) = f(x) + f(y)$.
On pose $a = f(1)$.}
\begin{enumerate}
  \item \question{Montrer que : $\forall\ x\in\Q$, $f(x) = ax$.}
  \item \question{On suppose $f$ continue. Montrer que : $\forall\ x \in \R$, $f(x) = ax$.}
  \item \question{On suppose que $f$ est bornée au voisinage de 0.
    Montrer que : $\forall\ x \in \R$, $f(x) = ax$.}
\end{enumerate}
\begin{enumerate}

\end{enumerate}
}